# Backend API URL for Local Development Only
# In production, CloudFront routes /api/* to ALB automatically
# Set this only when running frontend locally and need to connect to backend
VITE_API_URL=http://localhost:5000
